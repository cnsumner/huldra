name: huldra
description: A sample command-line application.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  path: ^1.6.0
  moor:
  moor_ffi:
  nyxx: ^0.30.0
  yaml_config: ^0.1.2

dev_dependencies:
  build_runner:
  moor_generator:
  pedantic: ^1.8.0
  test: ^1.6.0
